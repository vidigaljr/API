Para conectar a API ao Servidor Mongodb Atlas deve estar conectado ;

Na sequencia deve debugar o servido node, com o comando "node --wahtch server.js";

Após conectar a API, rodar no projeto de formulário.
